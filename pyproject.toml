[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pokemon-go-web-edition"
version = "2.0.0"
description = "Enterprise Pokemon GO Web Game and Distributed Simulation Platform"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
]
